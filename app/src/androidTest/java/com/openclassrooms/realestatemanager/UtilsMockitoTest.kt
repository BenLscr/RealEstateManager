package com.openclassrooms.realestatemanager

import org.hamcrest.CoreMatchers.`is`
import org.junit.Assert
import org.junit.Assert.assertThat
import org.junit.Assert.assertTrue
import org.junit.Test
import org.junit.runner.RunWith
import org.mockito.junit.MockitoJUnitRunner
import java.text.SimpleDateFormat
import java.util.*
import org.mockito.Mock
import org.mockito.Mockito
import org.mockito.Mockito.`when`
import org.mockito.Mockito.mock

@RunWith(MockitoJUnitRunner::class)
class UtilsMockitoTest {

    /*@Test
    fun todayDate_isInACorrectFormat() {
        val dateString = "11/09/2019"
        val mock = mock(Date::class.java)
        `when`(mock).thenReturn(Date(1568236087527))

        //assertThat(Utils.todayDate, `is`(dateString))
        assertTrue(Utils.todayDate == dateString)
    }*/

}